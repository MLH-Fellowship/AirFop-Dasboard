import React from 'react'

const ReportProjectList = ({projects}) => {
    return (
        <>
            {/* {projects && projects.length > 0 && 
            (
                projects.map(
                    (project)=> (
                        <div>
                            {project.title}
                        </div>
                    )
                )
            )} */}
        </>
    )
}

export default ReportProjectList
